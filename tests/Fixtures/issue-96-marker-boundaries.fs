module Issue96MarkerBoundaries

// TODO: deliberate development marker for issue 96 coverage.
(* FIXME *)
// HACK: temporary workaround
let assertion = Debug.Assert(true, "sanity")

let hackathon = 1
let hacker = 2
let todoList = 3
let undoSomething () = 4
let hasAHackerMindset = true

let probe () = traceDump "state"
let traceDumpster = 5
